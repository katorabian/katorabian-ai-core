service.chat      — orchestration only, no business rules
service.model     — model lifecycle + routing
service.prompt    — system prompt construction only
service.input     — user intent / commands
storage           — persistence (in-memory for now)
